import os

DEBUG = False
CLEANUP = 'always'

# for tests, the data folder will be set to <project>/tests/.workspace
DATA_FOLDER = os.path.join(os.getcwd(), '.workspace')

# credentials
CREDENTIALS = [
    {
        'name': 'beaker',
        'keytab': 'keytab_file',
        'keytab_principal': 'keytab_principal',
        'username':'username',
        'password': 'password'
    },
    {
        'name': 'openstack',
        'auth_url':'https://localhost:13000/v2.0',
        'tenant_name': 'tenant_name',
        'tenant_id': 'tenant_id',
        'username': 'username',
        'password': 'password'
    }
]
