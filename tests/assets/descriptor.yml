---
name: descriptor
description: descriptor

credentials:
  - name: provider

include:
  - ' '
  - malformed_included_file.yml

provision:
  - name: {{ asset_name | default('test') }}
    provider:
      name: openstack

orchestrate:

execute:

report:
